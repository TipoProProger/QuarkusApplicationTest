version: '3'
services:
  nginx:
    build: 
        context: ~/AngularTest
        dockerfile: ~/AngularTest/Dockerfile
    ports:
        - "80:80"
  net:
    build:
        context: .
        dockerfile: ~/src/main/docker/Dockerfile.jvm
    ports:
        - "8080:8080"
        
